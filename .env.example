# Copy this file to `.env` and fill in your own values.
# Never commit the real `.env` file — it is ignored by .gitignore.

# OpenAI API key used by langchain_rag_pdf.py
OPENAI_API_KEY_FREE=sk-your-key-here
